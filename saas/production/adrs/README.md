# Omnigent SaaS production ADR set

These eleven records define the P0 production decision boundary for candidate
`omnigent-saas-p0-sole-owner-waiver-2026-08-09`. The source of truth for candidate revisions and
the exact decision-file list is `../adr-approval-candidate.json`; its digest is
verified by CI. The compact ADR registry in `../baseline.json` must agree with
these documents.

`Accepted under sole-owner risk waiver` declares the explicit governance
downgrade selected for this candidate. Repository Owner `Dream1216` assumes all
technical-owner decisions without claiming independent Reviews or separation of
duties. The effective registry status becomes `Accepted` only after the merged
decision PR and exact-head CI are bound into an immutable waiver record. Any
material document edit creates a new decision bundle and invalidates that
record. Production verification gates are not waived.
